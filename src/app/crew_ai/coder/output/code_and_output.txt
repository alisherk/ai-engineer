```python
# factorial.py

def factorial(n):
    """
    Calculate the factorial of a number n.
    
    :param n: Non-negative integer to calculate the factorial of.
    :return: Factorial of n.
    """
    if n < 0:
        raise ValueError("Factorial is not defined for negative numbers.")
    elif n == 0:
        return 1
    else:
        result = 1
        for i in range(1, n + 1):
            result *= i
        return result

# Test the factorial function
if __name__ == "__main__":
    test_values = [0, 1, 5, 10, 20]
    outputs = {n: factorial(n) for n in test_values}
    
    for n, output in outputs.items():
        print(f"The factorial of {n} is: {output}")
```

Output:
```
The factorial of 0 is: 1
The factorial of 1 is: 1
The factorial of 5 is: 120
The factorial of 10 is: 3628800
The factorial of 20 is: 2432902008176640000
```

In the provided code, I implemented a `factorial` function that handles non-negative integers and raises an error for negative inputs. The main section of the code tests the function with several values, showcasing how the function operates and displaying the results of the factorial calculation. The printed output demonstrates the correct calculations for each test case. This script can be saved in a text file named `factorial.py` for future use or further testing.